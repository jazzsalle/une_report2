{
  "name": "@une/contract-tests",
  "version": "0.1.0",
  "private": true,
  "description": "CC-115 contract tests: T3Q legacy fixtures vs contract schemas, field gap matrix drift, capability governance, no-UNI-plan-fallback guard. Pure file-based — no DATABASE_URL needed.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@une/domain": "workspace:*",
    "@une/hwpx-engine": "workspace:*",
    "@une/provider-adapters": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.0",
    "yaml": "^2.9.0"
  }
}
