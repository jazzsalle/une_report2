# Implementation Status

Last updated: 2026-08-12

| ID | Title | Status | Branch/PR | Evidence | Next action |
|---|---|---|---|---|---|
| CC-000 | Confirm implementation profile and external assumptions | DONE | main | 00_DECISIONS_TO_CONFIRM.yaml APPROVED 2026-07-30; ADR-19; TECHNOLOGY_PROFILE.md; OB-09 closed, OB-14 added | Proceed to CC-001 (NestJS monorepo bootstrap) |
| CC-001 | Bootstrap monorepo and shared tooling | DONE | main | pnpm build/typecheck/test/lint all pass (10 tests); validate_handoff PASS 254 files; env examples + README | Proceed to CC-002 (compose: PostgreSQL+MinIO) |
| CC-002 | Bootstrap PostgreSQL, object storage, and local compose | DONE | main (PR #2 merged, CI green) | Runtime verified on WSL2 Ubuntu + Docker CE 29.6.2 (b5e0e27 + review fixes): healthy/pg_isready/mc ready; volumes survive down/up + WSL restart; 127.0.0.1 bind; une_app role (NOSUPERUSER/NOBYPASSRLS); bucket-scoped MinIO service account; no secrets; docs/evidence/CC-002-runtime-verification.md; dual review mandatory findings fixed same day | Proceed to CC-004 |
| CC-003 | Import and validate OpenAPI and JSON Schemas | DONE | main (PR #1 merged, CI green) | validate:contracts PASS (4 OpenAPI + 7 schemas + mock sync 13 routes); negative tests exit 1; 4 generated types-only files + CI drift gate; ADR-20; dual review conditions fixed | Proceed to CC-004 (needs CC-002 runtime) |
| CC-004 | Apply initial database migrations and seed | DONE | feature/CC-004 | node-pg-migrate v9 (ADR-21); 0001~0011 apply to empty DB (57 tables, FORCE RLS 17, une_app grants); baseline defects fixed pre-application (uuid[]/jsonb, plan timestamps, 74 random defaults, uk_outbox_idem, global-row policies, preflight guard); @une/db-integration 17/17 (outbox atomicity, RLS as une_app, idempotency dup rejected); data dictionary 57/512 + CI db-verify drift gate; docs/evidence/CC-004-migration-verification.md; dual review mandatory findings fixed same day | Create PR and merge; then CC-100 (needs CC-003+CC-004) |
| CC-100 | Implement mock authentication, tenant, and RBAC | DONE | feature/CC-100 | UNE-AUTH-001~007 (mock JWT AUTH_MODE=mock + env secret, tenant forgery blocked on 5 paths, RBAC from DB via role_permission, audit LOGIN/LOGOUT/REFRESH/ACCESS_DENIED); migrations 0012 (role_permission + 54-permission/15-role catalog, ADR-22) + 0013 (catalog read-only, refresh_hash unique); @une/api 55/55 (e2e 15 as SET LOCAL ROLE une_app), @une/db-integration 25/25; contract TokenResponse envelope + /auth/refresh PUBLIC_REFRESH updated with impl; dual review mandatory findings (correlation-id width 500, refresh rotation conflict, suspended tenant, permission catalog writable) fixed same day; docs/evidence/CC-100-auth-rbac-verification.md | Create PR and merge; then CC-110/CC-200 (G2 items) |
| CC-110 | Implement Plan and immutable PlanContextSnapshot | DONE | feature/CC-110 | UNE-PLAN-001~008 (plan CRUD + If-Match/version_no optimistic lock + trash, draft relaxed/snapshot strict AJV validation, canonical SHA-256 hash + version max+1 + supersedes + dedupe, DRAFT→CONTEXT_READY via domain transition fn, approval-locked 412, audit PLAN_CREATED/UPDATED/DELETED/PLAN_CONTEXT_SAVED/CONTEXT_SNAPSHOT_CREATED with before_json); Idempotency-Key common interceptor + migration 0014 api_idempotency (59th table, ADR-23) + uk_plan_context_draft_plan + plan.start_mode; contract plan slice finalized (envelope schemas, query params, 412/428 responses, IdempotencyKeyRequired, PLAN-412-002) + mock-server plan sync; @une/api 107/107 ×5 consecutive (e2e 38: plan 23 + auth 15), @une/db-integration 30/30; dual review B1(replay path-identity)+필수 6+MAJOR 7 all fixed same day; docs/evidence/CC-110-plan-context-snapshot-verification.md | Create PR and merge; then CC-115 (lowest-number-first) |
| CC-115 | Baseline current T3Q RPT-001/002 and target-v2 change request contracts | DONE | feature/CC-115 | target-v2 fixed editorially to 1.0.1-request (user-approved; allOf+additionalProperties defect made 4/5 request schemas unsatisfiable) + 10 schema-valid examples; validate:contracts examples section (all-ops-minus-exemptions, 2xx-only credit, legacy transcript SHA-256 pin) with 5 negative reproductions; new workspace @une/contract-tests 26/26 (legacy fixtures 13 incl. PlanContext-alignment/gap proofs, field-gap-matrix drift 5, capability governance 6 pinned to source via vitest alias, no-UNI static guard 2); capability registry 14 features all MOCK_ONLY + describeCapability (promotions gated on evidence+OPEN_BINDINGS+dual review); docs/handoff/T3Q_PLAN_FIELD_GAP_MATRIX.md; no migration (ADR-24 D6); port deferred to CC-125 (ADR-24 D8); dual review (B1+M3/F2 etc.) fixed same day; docs/evidence/CC-115-t3q-contract-baseline-verification.md | Create PR and merge; then CC-120 (needs CC-115) |
| CC-120 | Implement T3Q RPT-001 TOC job using mock adapter | DONE | feature/CC-120 | UNE-PLAN-009~015 (job create/status/SSE/cancel/retry + user TOC versions); worker polling plane per design 10 §4.2/§7.9 — migration 0015 une_worker role + conditional dispatch RLS policies (terminal writes tenant-scoped only, DB-enforced), 3-tx runner (dispatch/preconditions/provider-outside-tx/result), cancel sweep, lease reclaim, maxAttempts; deterministic MockLegacyT3qTocAdapter behind narrow T3qTocPort (mapper from CC-115 gap matrix, fixtures round-trip; port absorbed by CC-125); domain plan module (job state machine, toc-tree validate/deterministic node keys/flatten/content-hash, pure SHA-256, TocJobRequest seam); 2-layer idempotency (interceptor + uk_job_idempotency hash incl. planId); SSE manual streaming (Nest @Sse no-await defect found; public/internal event split, Last-Event-ID resume); dual review B1(user-edit protection race)+M1~M6+QA 필수4 all fixed same day; @une/api 175/175, worker 12/12, db-integration 41/41, domain 31/31, contract-tests 26/26, baseline 6, CI db-verify +build+worker job; ADR-25; docs/evidence/CC-120-t3q-toc-job-verification.md | Create PR and merge; then CC-125 (port unification + real Legacy/TargetV2 adapters) |
| CC-125 | Implement dual Legacy and Target-v2 T3Q Plan adapters with mocks | DONE | feature/CC-125 | Unified T3qPlanProvider port (op vocabulary complete, TocCapable/ContentCapable mixins, T3qPlanResult envelope with per-call adapterId/mappingVersion; T3qTocPort absorbed per ADR-25 D3); LegacyT3qPlanAdapter real HTTP (undici split timeouts 5s/60s UNE-baseline, fail-closed base URL/auth per OB-01, pre-response-only retry ×1 + Retry-After, per-op circuit breaker, lease>call-budget startup check) verified by 20 node:http fixture-server cases; TargetV2T3qPlanAdapter tocV2 202→poll→COMPLETED in-process mock, request machine-validated vs TocGenerationRequest (unevaluatedProperties typo negative); UNE_T3Q_PLAN_ADAPTER env selection (old var hard-fails, prod+mock blocked); guard-violation raw loss fixed (failures carry rawResponse — worker e2e regression); provider.requested emission; migration 0016 child-table RLS (ADR-25 D2 closed, EXPLAIN pinned); capability legacyToc→UNE_ADAPTER_READY, legacyContent/tocV2 MOCK_ONLY + CR-T3Q-* governance invariant; v2 contract examples-only +2 (version unchanged), exemptions 3→2; dual review same-day fixes (arch M1 ContentDraft wording+provenance slots, M2 runtimeMode + live-placeholder fail-closed mechanism, M3 describeRuntimeCapability MOCK RUNTIME marking, minors 11; QA PASS WITH CONDITIONS — all numbers independently reproduced, R1-R3/R5-R6 applied incl. 503/403 tests); provider-adapters 67/67, domain 35/35, contract-tests 38/38, worker 24/24, db-integration 51/51, api 175/175; ADR-26; docs/evidence/CC-125-t3q-dual-adapter-verification.md | Create PR and merge; then CC-130 (content job, needs ContentDraft/adapters from this item) |
| CC-130 | Implement T3Q RPT-002 content job and protected blocks | DONE | feature/CC-130 | UNE-PLAN-016 detailed (targetNodeKeys, protectedBlockIds persisted as USER_LOCKED, contentSummary result, content.block/job.progress SSE vocabulary, mock 19 routes); migration 0017 generated_block (60th table — §3.3/x-db-tables baseline-gap fix per ADR-27 D2; immutable rows + generation supersede with partial-unique current, protection_state from 0003 vocabulary, citations_json + citation_count, EXISTS-plan FORCE RLS, une_worker protection trigger); ContentJobRunner (3-tx, anchor by position+normalized-title double match with whole-response quarantine, B0/B1 protection re-check + outline-move fail-closed/discard, block events synthesized per US-PLAN-012 A-02, progress throttled); shared plan-jobs dispatch primitives extracted (ADR-25 D12 settled), PlanJobPoller; job-type-aware cancel/retry + active-job invariant job-type-agnostic + TOC-regen blocked while blocks exist (ADR-27 D9); partial retry = new job via targetNodeKeys (blockIds stays 400); legacyContent → UNE_ADAPTER_READY (ADR-26 D7 조건 충족); self-found defects fixed (toc_node rebuild ORDER BY level; supersede-before-insert; trigger STORED-column NULL by DB agent); dual review same-day fixes (arch B-1 scoped-regen coordinate corruption → full-outline coordinate injection + deep-node e2e, M-1 discard-path plan stuck, M-2 manual outline save guard, M-3 422 unification, M-4 PRESERVED hash payload, M-5 contract sync, 11 minors; QA PASS WITH CONDITIONS F1~F5 + G1~G9 applied incl. CI pytest baseline); domain 52, provider-adapters 67, contract-tests 38, db-integration 65, worker 31, api 193, baseline 10; ADR-27; docs/evidence/CC-130-t3q-content-job-verification.md | Create PR and merge; then CC-135 (target-v2 mocks) |
| CC-135 | Implement target-v2 plan job, semantic edit, evidence, and validation mocks | DONE | feature/CC-135 | Full target-v2 in-process mock behind the unified port — every capability stays MOCK_ONLY (contract NOT T3Q-accepted, OB-10/11; ADR-28 D11): port mixins SemanticEditCapable/EvidenceSearchCapable/ValidationCapable/JobLifecycleCapable under the unchanged 6-op vocabulary (lifecycle reports as jobStatus, ADR-28 D2) + T3Q_CONFLICT + describeRuntimeFeature; MockTargetV2JobStore single ledger (poll/SSE frames/cancel/partial-retry cannot disagree; idempotent resubmit joins same generation, payload-mismatch 409, retained-job cap 500); `.assumed` SSE framing (id==sequence, heartbeat comments, Last-Event-ID replay, terminal-event-required; resume-past-terminal = empty-frames success per QA F-3); content mapper joins section blocks onto outline-parallel ContentDraft tree (ADR-28 D7, lossy fields in raw), protectedBlockIds sent only when caller provides keys (id-space binding open — CC-150); ChangeProposal 3 targetTypes/4 opTypes with pre-reject + response-side protected re-check incl. recursive payload scan (ADR-28 D8 mechanism); evidence provenance fills ADR-26 D4 slots and persists to generated_block.citations_json (worker e2e + db-integration catalog pin — migration count 0); validation 6 deterministic heuristics, verdict gates NOTHING (ADR-28 D9, ADR-27 D8 corrected); capabilities discovery deep-equals contract example, providerBuild une-mock-*; worker: CC-130 m-10 trace seam closed, partial failure writes NO row and audits failed count (arch M-1), startup describeRuntimeFeature lines (AC5); adapter unions declared∪observed failures (arch M-2); live transport = constructor opt-in only (QA F-4); contract examples 12→22, exemptions 2→0, version unchanged 1.0.1-request, generated types diff 0; dual review same-day all applied (arch 0 BLOCKER/2 MAJOR/9 MINOR; QA PASS WITH CONDITIONS F-1~F-4 + G-1~G-9); domain 52, provider-adapters 108, contract-tests 60, db-integration 68, worker 33, api 193, baseline 10; ADR-28; docs/evidence/CC-135-target-v2-mock-verification.md | Create PR and merge; then CC-140 (HWPX import/IR shell — next lowest, deps CC-001) |
| CC-140 | Implement HWPX import, provenance, IR, and prototype analysis shell | DONE | feature/CC-140 | rhwp is still NOT imported — this item builds the GATE that makes intake enforceable (ADR-29 D1, OB-12): `scripts/validate-source-intake.mjs` R1..R11 wired into CI verify (provenance schema 20 fields + poc_gate, floating-ref ban, tree_digest blocks in-place upstream edits, submodule bypass, notices drift, SBOM presence, import-before-intake ban), green in the pre-intake state (R11) with 40 negative-fixture cases proving each rule actually fails. Engine (TypeScript per ADR-29 D3, ZERO new runtime deps — own central-directory ZIP reader and pull XML parser): package analysis (mimetype/version/container/hpf cross-check, SourcePreservationMap, unmanifestedParts), Document IR with deterministic stable ids and `partPath#el[n]` anchors (ADR-29 D6), template analysis (style signature, outline patterns keeping leading whitespace verbatim, static regions, prototypes, Spec §1.5 confidence), and a data-driven 2-layer classification. Type ownership split (ADR-29 D4): IR/vocabulary/roll-up/confidence live in @une/domain, the engine consumes them; JSON Schemas in contracts/schemas with a schema↔union drift guard. Losslessness proven three ways (ADR-29 D7): I4 coverage (known ∪ unknown == every ZIP entry), I5 byte preservation, and in-memory no-edit reconstruction equivalence — RT-A data sufficiency established before the Package Writer exists (CC-160). Corpus = 6 REAL HWPX forms in templete/ resolved by sha256 + 9 synthetic negatives; real-document finding: content.hpf's manifest omits BinData/Scripts/Preview/META-INF, so non-manifested parts carry the losslessness burden. Self-found and fixed during implementation: the AUTO verdict was structurally unreachable — package parts every HWPX carries were capping the verdict, and layout/whitespace elements (hp:colPr, hp:fwSpace, hp:lineBreak) were classified as unsupported objects; fixed by scoping roll-up rule 3 to ELEMENT and promoting benign constructs to explicit rules, with catch-all hits pinned at 0 and cap CAUSES (not just labels) frozen in the golden table; AUTO reachability then proven by a synthetic A/B pair at identical confidence. Also corrected outline-level inference (hc:intent is a negative hanging indent — leading whitespace is the real level signal). Migrations: 0 (ADR-29 D9, which also registers the document child-table RLS gap as a blocking prerequisite for CC-150). dual review same-day all applied (arch 1 BLOCKER/8 MAJOR/9 MINOR; QA FAIL→F-1~F-5 + G-1~G-4 applied) — the BLOCKER was a manifest key the loader rejected, which had silently disabled the 94-case corpus regression, so the earlier 196 figure was never actually run; verified numbers below come from a single `pnpm test`: domain 62, hwpx-engine 238, contract-tests 152, db-integration 68, provider-adapters 108, api 193, worker 33, validate:intake(R1~R12)/contracts/handoff PASS, baseline 10; ADR-29; docs/evidence/CC-140-hwpx-ir-verification.md | Create PR and merge; then CC-150 (Revision/ChangeSet — needs the 0018 child-table RLS migration first, and an rhwp editor-fit check) |
| CC-150 | Implement document Revision, ChangeSet, autosave, diff, and conflict | DONE | feature/CC-150 | Server-side edit core only (editing UI needs rhwp, still not imported — OB-12; HWPX save/export is CC-160). Migrations 0018 (RLS on the eight document child tables — closes the blocking prerequisite ADR-29 D9 registered) and 0019 (document_autosave as the 61st table, undo lineage columns, CHECK constraints, idempotency UKs, indexes). Domain: selection (three layers pinned by types, Offset Normalization Contract), change-set (8-op vocabulary, NodeAlias, 10 rejection reasons), Document IR v2 (NodeProvenance discriminated union so a node with neither anchor nor hint cannot compile). Engine: SelectionResolver, ChangeSetExecutor, inverse-op derivation, prototype resolve, checkEditInvariants wired before commit — atomicity by data structure rather than rollback (the failure result type has no `ir` field). API: UNE-DOC-005~009 with ETag + baseRevisionId double guard, conflict payload via ETag header + meta, idempotency, document audit actions, materialize with ADR-27 D4 triple defence. Three defects found by measurement during implementation: CC-140's stable ids were anchor-derived and collapse after an edit (fixed by freezing ids and deriving authored ids separately); the §1.9 inverse table is wrong for character deletion and SPLIT (all six corpus documents mismatched by hash); RLS made an unindexed child scan 30x slower (173ms -> 1.2ms after 0019). Dual review (parallel, opus) opened arch 2 BLOCKER/6 MAJOR/7 MINOR and QA FAIL; both independently found the same three: unvalidated `restore` injection from the request surface (forged SOURCE anchors, locked nodes, and a 500), an Undo path that could not execute (the returned inverse ops violate the response schema), and a materialize source bypass. All applied same day (ADR-30 D16): Undo now names the ChangeSet to reverse instead of resubmitting operations, UNDO_CONFLICT implemented, aliases are not followed for nodes that still exist, GENERATED_BLOCKS limited to one source, ir_hash dedup implemented, REJECTED replays answer 422 again, document.status enforced. Numbers below come from a single `pnpm test` (exit 0) with DATABASE_URL set: domain 62, hwpx-engine 353, contract-tests 188, api 242, db-integration 107, provider-adapters 108, worker 33, baseline 10; contracts/intake/handoff PASS, build/typecheck/lint/format PASS, contract-type regeneration diff 0; ADR-30; docs/evidence/CC-150-document-edit-verification.md | Create PR and merge; then CC-160 (HWPX preservation export + Track A) |
| CC-160 | Implement HWPX preservation export and Track A validation | DONE | feature/CC-160 | Rewriting is **byte-range splicing over the original XML**, never re-serialization, and ZIP entries that were not replaced are copied as stored bytes (never recompressed) — so with no edits the output is **byte-identical** to the input, proven on all six real corpus documents and again end-to-end through the worker (AC1). Track A defines 16 `RTA-*` checks over four layers (design had an intake code table but none for saving, ADR-31 D5) and reports the three layers it does NOT run with reasons; a FAIL discards the bytes rather than shipping them with a flag (D6, HWPX-1105 covered by test). Where a rewrite target is not unique the engine refuses (HWPX-1103) instead of guessing — a real document carries `[hp:t, hp:ctrl, hp:ctrl, hp:t]` runs where a form field splits the text, and a wrong HWPX simply opens. Paragraph insert (prototype clone) and delete are implemented and tested on all six documents. Migrations 0020 (export_job.tenant_id — the asymmetry with generation_job made worker settlement impossible; export/validation vocabularies; validation_report append-only; the two constraints ADR-30 deferred) and 0021 (started_at/attempt_no lease, file_object column-level immutability). New object-storage port with S3/MinIO + in-memory adapters keeps the SDK behind the adapter boundary. **Four defects found by first real use or by review**: the MinIO service account had never had its policy attached (the CC-002 guard matched `une-app` against an access key literally named `une-app-<random>`, so every request was 403); `DocumentImportService` never registered source bytes, leaving `source_file_id` NULL and preservation export structurally impossible; Track A compared paragraphs **by id** across a rebuild, so insert/delete failed on every document (the ADR-30 D2 defect class resurfacing in the validator); and the worker lease keyed on `created_at` (request time) rather than claim time, letting a second worker re-claim in-flight jobs. Dual review (architecture 8 MAJOR/7 MINOR, QA FAIL 6 필수) applied same day. Tests: hwpx-engine 423, api 257, db-integration 120, provider-adapters 128, worker 44, contract 188, domain 62. **PR #13에서 CI가 처음 돌자 생성 타입 drift 게이트가 실질 결함을 잡았다**: 계약을 고친 뒤 `pnpm generate:contract-types`를 다시 돌리지 않아 커밋된 타입에는 계약에서 제거된 `DocumentExportRequest.options.saveMode`가 남아 있고 계약에 있는 410 Gone(UNE-DOC-014)·`application/hwp+zip`·`checks[].layer` optional이 빠져 있었다. 이 대조 게이트는 `ci.yml`에만 있고 `pnpm test`에는 없어 PR을 열기 전에는 드러날 수 없었다(재생성 후 전체 게이트 재실행 exit 0, `3856084`). ADR-31 파일이 XML 금지 제어문자 한계를 적으며 예시를 실제 NUL/백스페이스 바이트로 써 git이 binary로 판정하던 것도 함께 교정(`2053e6d`). | ADR-31; docs/evidence/CC-160-preservation-export-verification.md | **완료 — PR #13 머지 커밋 `e8d8b78`로 머지(2026-08-07), main CI PASS.** |
| CC-170 | Complete Plan vertical slice E2E | DONE | feature/CC-170 | 착수 시점에 인수기준의 경로에 **진입점이 없었다**: UNE-DOC-001~004가 계약에 자리표시자로만 있고 컨트롤러가 없었으며(ADR-31 D1이 CC-160에서 제외한 뒤 주인이 없었다), `DocumentImportService`는 테스트만 호출했고, apps/web은 4개 파일 셸이었다. 그래서 통합 항목이 아니라 구현 항목이 됐다. **업로드 3단**(사전등록→직접 전송→완료확정)을 설계대로 만들고 검증 지점을 UNE-DOC-002 하나로 두었다 — 저장된 바이트에서 크기·SHA-256을 재계산하고 엔진 `analyzePackage`로 내용 기반 HWPX 판정(확장자·Content-Type 불신), 실패는 ABORTED 종단. presign은 선언 해시를 **서명에 넣어**(x-amz-checksum-sha256) 저장소가 다른 바이트를 직접 거부하게 했고 실 MinIO에서 확인했다. presign 불가 드라이버는 예외가 아니라 null을 돌려주고 API가 서명된 1회용 티켓 라우트로 대체한다(Bearer 없음 — presign과 같은 성질, 티켓의 테넌트 claim으로 RLS 스코프를 연다). 마이그레이션 0022: `upload_state`/`verified_at`(업로드 검증은 AV 검사와 **다른 축** — scan_status를 CLEAN으로 올리면 하지 않은 검사를 했다고 감사에 남는다, OB-15), 기존 행 VERIFIED 백필(테스트로 확인), 앱 롤에 두 컬럼만 UPDATE 허용(sha256/storage_key 불변 유지), `plan.document_id` 부분 유니크. **`document.plan_id`를 만들지 않았다** — plan.document_id가 0003부터 있고 FK도 있는데 쓰는 코드만 없었다(계획을 구현 중 뒤집었다). **`malware_scan` 드리프트 종결**: 계약이 존재하지 않는 테이블을 가리켰다. 테이블을 만들지 않고 계약을 교정하고, **구현된 API의 x-db-tables 전건을 데이터 사전과 대조**하는 계약 게이트를 세웠다(구현 여부는 컨트롤러 주석에서 유도). **슬라이스 UI** 6화면(로그인→계획서→기준정보→반입→목차·본문→Export·다운로드), 신규 런타임 의존성 0, 타입은 계약에서 생성(apps/web/src/generated 신규 대상), CORS는 env allowlist(와일드카드 기동 거부). 편집기 화면은 없다(rhwp 미반입 OB-12). **presign 공개 엔드포인트 결함**: OBJECT_STORAGE_PUBLIC_ENDPOINT가 CC-001부터 문서화됐으나 읽는 코드가 없었다 — 컨테이너/호스트 주소가 갈리는 배포에서 서명 URL이 브라우저에서 연결조차 되지 않는다. SigV4가 Host를 서명하므로 전용 서명 클라이언트로 해소. **신규 워크스페이스 @une/e2e**가 API와 워커를 한 프로세스에서 돌려 전 구간을 지난다(편집 단계는 편집기가 없으므로 materialize가 대신한다): 받은 바이트의 해시 == 검증 보고서 outputSha256, 상관관계 ID가 전 구간 감사를 묶음, ValidationReport 16검사 + 미실행 3계층 사유, 대안·오류 7경로. **슬라이스가 처음 밟아 드러난 되쓰기 결함 3건**(전부 엔진에서 수정 + 회귀 테스트): ① 여러 문단 삽입 시 두 번째부터의 anchorHint가 AUTHORED를 가리켜 **실체화한 문서는 통째로 Export 불가**였다(CC-160은 문단 하나만 시험) ② 되쓰기가 앵커 이웃을 복제해 IR이 정한 서식이 무시되고 RTA-STY-001이 FAIL ③ 실문서 OUTLINE_1 프로토타입은 run 4개+빈 run이라 복제기가 거절. **성능 기준선**(합성 50쪽=2,000문단, 문단40=1쪽 가정, 개발PC): 분석 p95 256ms(목표 5,000 PASS) · 업로드+반입 p95 613ms(PASS) · 편집 적용 p95 162ms(목표 300 PASS) · Export+TrackA p95 352ms(기준선) · 다운로드 p95 8ms(기준선). 성능 테스트는 게이트가 아니다. 화면 캡처는 `pnpm --filter @une/e2e screens`가 남기며 **CI 게이트가 아니다**(브라우저 바이너리). 캡처가 UI 결함 2건을 잡았다(UNE-PLAN-007 요청 형태, UNE-PLAN-014 tocTree 누락). 테스트: hwpx-engine 426(+3) · api 283(+26) · provider-adapters 137(+9) · db-integration 127(+7) · contract 195(+7) · e2e 13(신규) · web 24(+23) · worker 44 · domain 62 · baseline pytest 14(+4). 마이그레이션 22개, 테이블 61 유지, 데이터 사전 61/574 **CI 최초 실행 결과: PR #14가 `verify`·`db-verify` 둘 다 통과** — 슬라이스 E2E(`@une/e2e`)가 CI에서 처음 돌아 녹색이다(리뷰 필수1의 목적 달성). | ADR-32; docs/evidence/CC-170-plan-slice-e2e.md | **완료 — PR #14 머지 커밋 `0eabd59`로 머지(2026-08-07), main CI PASS. G2 종료.** then CC-200 (Situation/SituationFact — G3 진입) |
| CC-200 | Implement Situation and candidate SituationFact ingestion | DONE | feature/CC-200 | 상황 CRUD + 후보 SituationFact 수집. **종점은 `status=CANDIDATE`**이며 중복군·충돌 해소·Snapshot 확정(UNE-SIT-009~013)은 CC-210이다. 착수 시점에 두 가지가 열려 있었다: 0023(이전 세션)의 주석이 **존재하지 않는 ADR-33 D2/D8을 인용**하고 있었고, 계약의 `Situation`이 `additionalProperties: true` 자리표시자라 예제 게이트가 아무것도 검증하지 못했다(SIT-002는 설계가 Page라고 적은 자리에 단건을 가리켰고 SIT-004/007/008은 `GenericRequest`였다). **Provider 수집은 동기**(사용자 결정) — 어댑터가 전부 목업인 지금 비동기로 만들면 큐·리스·폴링이 아무것도 기다리지 않는 장치가 된다. 그래서 `provider_job`은 SUCCEEDED/PARTIAL/FAILED 셋뿐이고 `finished_at`이 NOT NULL이다(행이 종결된 채로 태어난다). 트랜잭션 경계: 짧은 읽기 → **트랜잭션 밖** Provider 병렬 호출 → 한 쓰기 트랜잭션(Job·원문·출처·Fact·상태전이·감사). 쓰기 트랜잭션은 상황을 `FOR UPDATE`로 다시 잠그고 재확인한다 — 호출 중 종결되면 412이고 부분 기록이 남지 않는다. **부분 장애는 200**(설계 06 US-SIT-005; 전멸해도 200 + FAILED Job들이고 수동 입력 경로는 열려 있다). 실패를 여덟 갈래로 나눠 `DISABLED`(플래그 off — SafeKorea/Naver, OB-05)와 `NOT_CONTRACTED`(부를 곳이 없음 — T3Q 상황 API, OB-02)와 실제 장애를 구분하고, **비활성 Provider도 조용히 건너뛰지 않고 FAILED 행을 남긴다.** 플래그를 켜도 어댑터가 없으면 `NOT_CONTRACTED`다. 정규화는 설계 06 US-SIT-006을 그대로 옮긴 3갈래 판별 유니온(NORMALIZED / ORIGINAL_KEPT / INVALID)이고 원문은 세 경우 모두 남는다. **수동 입력과 보정도 같은 정규화기를 지난다**(`3 cm` 보정 → `30 mm`). 시각은 명시적 오프셋을 요구하고 없으면 추측 대신 격리한다. 마이그레이션 **0024** 신설: 0023이 `situation`/`situation_fact`에 `updated_at`을 추가하면서 이 저장소 관례인 `trg_*_updated_at` 트리거를 빠뜨려(개발 DB 실측 — `plan`에는 있고 둘에는 없었다) 두 컬럼이 INSERT 시각에 영원히 고정되고 컬럼 주석이 거짓이 되던 것을 전진 마이그레이션으로 닫았다. 테이블 62 유지. 신설 API 둘(**UNE-SIT-014** 후보 Fact 목록 — 없으면 SIT-008이 보정할 factId를 얻을 방법이 없어 후보검토 화면이 성립하지 않는다; **UNE-SIT-015** Job 상태, SSE의 폴링 대체). **구현 중 실측으로 잡은 것 3건**: `new Date('2026-02-30')`가 3월 2일로 굴러가 존재하지 않는 관측시각이 저장되던 것, 0024의 트리거 누락, SIT-005가 계약(200) 대신 201로 나가던 것. **이중 리뷰**(병렬, opus) 초기 판정 arch 1 BLOCKER/6 MAJOR/8 MINOR, QA PASS WITH CONDITIONS(필수 5/권고 6) — 전건 당일 반영. 두 리뷰가 독립적으로 같은 결함 둘을 지목했다(응답 스키마가 `additionalProperties` 미설정이라 계약 테스트의 `.not.toBe(true)`가 아무것도 막지 못함, 오류코드가 설계와 다른 뜻으로 쓰임). 실질 수정: 컨트롤러의 시각 검사 **두 번째 사본** 제거(occurredAt이 달력 검사를 지나지 않았다), `Promise.all`의 무보호 어댑터 호출 봉인(한 어댑터가 던지면 다른 Provider의 원문·후보가 통째로 버려지고 Job 행이 하나도 안 남았다), 시험 훅 `mockScenario`를 운영 요청 경로에서 설정(`SITUATION_PROVIDERS` 토큰 주입 + `scenariosEnabled` 기본 off)으로 이동, `PROV-400-001`/`FACT-400-001` 신설(400에 '장애'·'정규화 격리' 코드를 재사용하고 있었다), 단위 미상을 추측하지 않도록 `UNIT_MISSING`+ORIGINAL_KEPT로 하향, `findFactKeySpec`을 (범주,Key) 쌍 조회로 변경, **Provider당 제한시간 도입**(없으면 동기 경로에서 느린 Provider 하나가 요청을 무기한 붙잡는다). 게이트 신설: 계약 어휘 3중 대조(0023 SQL 원문 ↔ 도메인 상수 ↔ 계약 enum), 오류코드 대조(구현 소스 ↔ x-error-codes ↔ COM-* 열거 — 재명명이 테스트 0건 실패로 통과했던 자리), 응답 스키마 7종 `additionalProperties: false` + 응답 예제 3건, x-db-tables 하한 31→40. 테넌트 격리 회귀(`situation-table-rls.test.ts` — 0023 §5 주석이 이미 이 파일명을 참조했다): 착수 시점 `situation`만 RLS가 켜져 있고 나머지 여섯은 정책이 한 번도 없었다(0011의 일괄 GRANT 때문에 정책 없음 = 전 테넌트 공개). 읽기·쓰기·불변·`une_worker` 42501 차단까지 단언. 수치는 트리를 얼린 뒤 단일 `pnpm test`(exit 0, skip 0)로 재현: domain 12/101 · db-integration 12/141 · provider-adapters 14/162 · hwpx-engine 23/426 · api 25/339 · worker 5/44 · contract 13/222 · e2e 2/13 · web 3/28. build·typecheck·lint·format PASS, contracts·intake·handoff PASS, 계약 타입 재생성 diff 0, 계약 예제 31건 통과. | ADR-33; docs/evidence/CC-200-situation-fact-ingestion.md | PR 생성 후 머지 커밋으로 머지; then CC-210 (중복군·충돌 해소·SituationSnapshot 확정 — `openConflictCount` 실계산, `SituationSnapshot` 스키마 실형태화, `fact_duplicate_group` 계약 드리프트 종결) |
| CC-210 | Implement duplicate/conflict resolution and SituationSnapshot | DONE | feature/CC-210 | 중복군 계산·충돌 해소·불변 SituationSnapshot(UNE-SIT-009~013). CC-200이 남긴 결정 세 개를 닫았다: `fact_duplicate_group` 신설(계약 UNE-SIT-009가 가리키던 두 번째 유령 테이블 — provider_result와 같은 결론), `situation_fact` 중복 유니크 키는 **만들지 않기로 결정**(중복은 제약이 아니라 판정이고, 키를 박으면 두 번째 Provider의 응답이 23505로 떨어져 중복이 아니라 수집을 막는다), 그리고 계약의 `SituationSnapshot` 자리표시자 해소 — **상황 계열에 열린 스키마가 0건이 됐다.** **UNE-SIT-008의 의미가 바뀌었다**(사용자 결정): 보정이 제자리 UPDATE에서 **파생 Fact 생성 + 원본 SUPERSEDED**로 전환됐다. 설계 06 US-SIT-007 #3·§7.1과 CLAUDE.md의 "never overwrite audit history"가 같은 말을 하는 자리였고 ADR-33 수용 한계 12가 그 어긋남을 기록해 두었다. 실질 이득은 확정된 Snapshot이 가리키는 근거의 값이 사후에 바뀌지 않는다는 것이다. 출처는 원본을 승계하지 않고 MANUAL/USER로 새로 만든다 — 사용자가 고친 숫자가 기상청 값으로 보이면 안 된다. `reason`이 필수가 됐고(0025 CHECK가 DB에서도 강제) CC-200 e2e 3건을 새 의미로 갱신했다. **충돌 탐지는 SIT-009 호출 시점**(사용자 결정) — 설계 10이 SIT-009에 strategy·threshold를 둔 것은 그룹화가 사용자가 고르는 계산이라는 뜻이고, 수집 시 자동으로 하면 전략이 고정되고 동기 수집 경로가 느려진다. **값도 시각도 같으면 충돌이 아니라 중복이다**(설계 06 US-SIT-006 #3). 해소는 원천을 건드리지 않는다 — 선택되지 않은 후보를 REJECTED로 내리지 않는다(A-01의 복수 병존). 확정은 **미해결 충돌을 차단**하고(412 SIT-412-003) 자동으로 하나를 고르지 않는다. 차단 사유는 여섯 갈래로 나눠 화면이 무엇을 하라고 말할 수 있게 했다. Snapshot은 **Fact의 사본**을 갖는다(참조만 두면 원천을 따라 값이 움직인다 — 설계 06 A-02) — 파생 Fact와 함께 두 겹의 방어다. 해시는 사실과 effectiveAt만 넣고 확정자·시각·버전·사유는 뺀다(그래야 "내용이 같은가"를 물을 수 있다, ADR-23 D4와 같은 규칙). Diff는 **표준 Key 기준**이다 — factId로 비교하면 근거를 바꿀 때마다 같은 사실이 삭제+추가로 보인다. **실행으로 잡은 결함 2건**: ① 해소한 충돌이 재계산 한 번으로 되살아났다(부분 유니크가 OPEN에만 걸려, 해소 행은 남아 있는데 판단이 없던 일이 되고 확정이 다시 막혔다) → 같은 후보 집합의 선행 충돌 검사로 해소하고, 후보가 달라지면 새로 연다 ② 그 수정의 INSERT…SELECT…NOT EXISTS에서 같은 파라미터가 양쪽에 쓰여 플래너가 타입을 정하지 못해 500(명시 캐스트로 해소). 마이그레이션 **0025**: 테이블 62→63, 파생 계보 3컬럼 + SUPERSEDED, 충돌·해소 어휘 CHECK와 불변 REVOKE, Snapshot 버전 유니크·해시 형식·빈 사실 금지, `situation.current_snapshot_id` FK(0004부터 FK가 없었다). **이중 리뷰**(병렬, opus) 초기 판정 arch 2 BLOCKER/11 MAJOR/9 MINOR, QA PASS WITH CONDITIONS(필수 7/권고 9) — 전건 당일 반영. 두 리뷰가 독립적으로 같은 것 다섯을 지목했다. BLOCKER 둘은 **인수기준 1을 우회하고 있었다**: ① 충돌의 정체성이 `fact_key` 하나라 같은 Key의 다른 시간창 충돌이 유니크에 걸려 조용히 사라졌고, 첫 충돌만 해소하면 미해결 불일치를 못 본 채 확정이 통과했다 ② 0025가 `SUPERSEDED`를 더했는데 도메인·계약·JSON Schema는 3값 그대로였고, ADR-33 D1의 삼중 대조 게이트가 **0023 한 파일만 읽어** 초록으로 통과했다 — 게이트가 막으려던 사고가 게이트를 우회했다. 그 밖의 실질 수정: 그룹화 키에 범주 누락(허위 충돌), 재확정이 이전 판의 사실을 잃어 **Diff가 지운 적 없는 사실을 REMOVED로 보고**, 재계산이 사라진 충돌을 닫지 않아 확정 영구 차단(→`OBSOLETE`), `openConflictCount` 하드코딩 0(→`CONFLICT_OPEN`이 도달 불가능했다), 재계산이 확정과 경합, 확정 승격 결과 미검사, 해소가 선택 대상 상태 미검사, 확정이 1000건에서 잘림, SIT-008이 다른 자원의 ETag 응답. 계약 정합 7건(SIT-012 201, 412 응답 선언, 질의 파라미터 — 없어서 인수기준 4의 진입점을 타입 클라이언트로 호출할 수 없었다). 게이트(트리 동결 후 단일 `pnpm test` exit 0, skip 0): domain 14/143 · api 26/375 · contract 13/222 · provider-adapters 14/162 · db-integration 12/141 · hwpx-engine 23/426 · worker 5/44 · e2e 2/13 · web 3/28. build·typecheck·lint·format PASS, contracts·intake·handoff PASS. 데이터 사전 63/601. | ADR-34; docs/evidence/CC-210-duplicate-conflict-and-snapshot.md | PR 생성 후 머지 커밋으로 머지; then CC-220 (지식문서 업로드 + UNI 목업 어댑터) |
| CC-210-FU | CC-210 후속: 확정 기준 가드 + Provider 페이로드 보존기간 | DONE | feature/CC-210-followup | 사용자가 닫기로 한 수용 한계 둘. **(1) Snapshot 확정에 직전 판을 명시한다**(`expectedSnapshotId` 필수, 불일치 시 409 `SIT-409-004`) — 없으면 같은 화면을 보던 두 사람이 **둘 다 성공**하고 뒤에 누른 사람이 앞사람의 확정을 못 본 채 기준 상황을 바꾼다. 행 잠금은 순서만 정할 뿐 그 사실을 알려주지 않는다. 첫 확정은 `null`, 필드 생략은 400(가드를 우회할 수 없다). ADR-34 D17. **(2) OB-16 종결 — 마이그레이션 0026.** 사용자 결정으로 1개월 뒤 **페이로드만 비우고 행은 남긴다**: `payload_sha256`·`item_count`·수신시각·상태가 남아 "무엇을 받았다고 주장하느냐"에 답한다(행을 지우면 그것도 사라진다). `provider_job.request_json`도 대상이다 — `query`는 자유형 객체라 주소·성명이 들어올 수 있는데 0023이 `une_app`의 UPDATE/DELETE를 회수해 **사후에 가릴 수단이 없었다**. `redacted_at` 신설("원래 비어 있었다"와 "기간이 지나 비웠다"는 감사에서 다른 사실이다) + 짝을 강제하는 CHECK. **0027(검토 반영)**: 0026의 CHECK는 한 방향만 막아 `raw_payload_json`을 임의 값으로 덮는 것과 이미 비운 행의 `redacted_at`을 지우는 것이 **둘 다 통과했다**(실측 — 0027 없이 먼저 돌려 실패를 확인한 뒤 막았다). `BEFORE UPDATE` 트리거가 허용 전이를 하나로 고정한다(되돌리기·재마스킹 없음). 소유자도 예외가 아니다 — 예외를 두면 "마이그레이션으로는 감사 기록을 고칠 수 있다"가 되어 append-only가 반만 참이 된다. **정리는 워커가 아니라 전용 롤 `une_retention`으로 돈다** — `une_worker`에 UPDATE를 주면 ADR-33 D2(워커는 상황 계열 테이블에 닿지 않는다, 42501 회귀로 고정)가 조용히 뒤집힌다. 권한 실측: 두 테이블 SELECT + 네 컬럼 UPDATE뿐, INSERT/DELETE 없음, `payload_sha256`은 전용 롤로도 42501. BYPASSRLS가 아니라 **롤 대상 정책**으로 전 테넌트를 보므로 근거가 `pg_policies`에 드러난다. 기간은 운영 설정(`UNE_PAYLOAD_RETENTION_DAYS`, 기본 30)이고 보존 롤이 워커 롤과 같으면 **기동을 막는다**. 테이블 63 유지, 마이그레이션 25→**27**. **결정 정본 ADR-35 신설** — 이 결정은 CLAUDE.md 비협상 규칙 둘(원문 보존, 감사 기록 덮어쓰기 금지)을 좁히는데 근거가 증거 문서뿐이었고 증거 문서는 소스오브트루스 순위에 없다. ADR-33 D2 따름정리는 **롤 경계이지 프로세스 경계가 아니다**로 개정, ADR-34 수용 한계 7·10은 D17로 닫힘 표시(그대로 두면 이미 막은 동시확정 구멍이 열려 있다고 읽힌다). `withRole(role)` → `withRetentionScope()`(호출부가 기동 가드를 우회할 수 없게), SQL은 `retention/retention.repository.ts`로 분리(ADR-27 D1의 다른 네 파이프라인과 같은 형태), 스윕 로그는 0건이어도 남긴다(0건과 미실행이 로그상 같아지면 안 된다). **QA 필수 지적(F1) 반영**: 스윕에만 `FOR UPDATE SKIP LOCKED`가 없었다 — 워커의 다른 네 파이프라인은 전부 갖고 있고 `main.ts`는 기동 즉시 한 번 스윕하므로 레플리카 둘이면 **부팅마다** 충돌한다. 0027 이전에는 나중 트랜잭션이 앞의 `redacted_at`을 덮었고(그 컬럼의 존재 이유가 사라진다), 0027 이후에는 트리거가 손상은 막되 **배치가 통째로 롤백**되고 OB-17과 구분되지 않는 42501만 남는다. SKIP LOCKED를 빼고 새 동시성 테스트가 실제로 깨지는 것을 확인한 뒤 넣었다. 스윕 결과에 **잔여 만료 건수**도 함께 싣는다(500행/6시간보다 유입이 많으면 백로그가 조용히 자란다). **작업 중 드러난 선재 결함(미수정)**: `une_app`은 `une_worker`/`une_retention`으로 `SET ROLE`할 수 없다 — 멤버십을 주는 마이그레이션·initdb·compose·CI가 하나도 없어 `.env.example`대로 띄운 워커는 첫 트랜잭션에서 42501로 죽는다. 0015/CC-120부터 잠복했고 테스트가 superuser로 강등해 드러나지 않았다. 값싼 해법(`GRANT une_retention TO une_app`)은 API 런타임에 전 테넌트 원문을 비울 능력을 준다 — 전용 롤을 만든 이유와 정면으로 어긋나므로 하지 않았다. **OB-17로 등재**했고 OB-16 종결 행에도 "스윕은 아직 돌지 않는다"를 함께 적었다(인계 표만 보는 사람이 Closed를 "정리된다"로 읽지 않도록). 게이트: build·typecheck·lint·format PASS, contracts·intake·handoff PASS. 테스트 전량 초록·skip 0 — domain 147(+4) · db-integration 154(+13) · worker 56(+12) · api 380(+5) · provider-adapters 162 · hwpx-engine 426 · contract 222 · e2e 13 · web 28. | ADR-33 수용한계 4 종결 · ADR-34 D17 · docs/evidence/OB-16-payload-retention.md | PR 생성 후 머지 커밋으로 머지; then CC-220 |
| CC-220 | Implement knowledge document upload and UNI mock adapter | DONE | feature/CC-220 | UNE-KNOW-001~003(등록·상태조회·재시도) + UNI 지식문서 포트·mock·실 HTTP 어댑터. **상태를 두 축으로 나눴다**(`status`=UNE 등록 축, `uni_status`=UNI 처리 축) — 0004의 주석은 한 컬럼이었지만 설계 06이 US-SIT-009와 US-SIT-010에 서로 다른 상태전이를 적었고, 합치면 UNI가 응답하지 않을 때 무엇이 참인지 말할 수 없다(ADR-32 D3과 같은 판단). `uni_status`의 NULL은 "아직 모른다"다. **UNI 호출은 워커가 한다**(설계 10 §7.23 7단계) — 등록은 202로 끝난다. 마이그레이션 **0028**(knowledge_document에 어휘·상관식·FK 신설 — 0004에는 제약이 하나도 없었다; provider_job에 QUEUED/RUNNING — 0023 §4가 예고한 그 마이그레이션), **0029**(0027의 트리거가 워커의 모든 UPDATE를 막아 규칙을 페이로드 컬럼으로 좁힘), **0030**(검토 반영 — 0028의 테이블 단위 UPDATE가 워커에게 마스킹 컬럼과 종결 잡까지 열어 줬다. **둘 다 실측**: 워커가 request_json을 마스킹하고 SUCCEEDED 잡을 QUEUED로 되돌렸다. 컬럼 GRANT + RESTRICTIVE 정책으로 좁힘). **실 어댑터는 미확인 값을 추측하지 않고 기동을 거부한다** — multipart 파일 필드명·로그인 토큰 필드명이 OB-13이며 요청 규격(`uni-knowledge-api-change-request-v1.yaml`, CR-UNI-001~007)과 사내 개발자용 설명서를 함께 냈다. **이중 리뷰**: arch BLOCKER 0/MAJOR 5/MINOR 9, QA **FAIL**(필수 9). 반영 완료 — F1 typecheck(e2e 하네스의 두 번째 ApiConfig 리터럴 누락, vitest는 타입검사를 하지 않아 테스트만으로는 보이지 않았다)·F2 format·F3 데이터사전 드리프트·**F4 재시도가 무의미했다**(UNI 처리 실패 재시도가 `uni_status='ERROR'`를 남겨 재업로드가 성공해도 영원히 폴링 대상에서 빠지고 근거 자격을 얻지 못했다; 상태가 안 변하니 동시 재시도 둘이 각각 잡을 만들어 UNI에 두 벌 — 재시도가 두 축을 모두 되돌리도록 수정)·**F5 ADR 문장이 거짓이었다**(갇힌 업로드의 복구 경로가 UNE-KNOW-003이라고 적었으나 UPLOADING은 409다 — 수용 한계를 사실대로 다시 씀)·F7 계약 오류코드 정합(던지지 않는 `UNI-503-001` 제거, COM-0400/0409 선언)·F9 환경변수 16개 문서화·M3 잡 클레임에 operation 필터(없으면 CC-240의 UNI 잡을 이 러너가 훔친다)·M4 mock 시나리오 훅을 설정 뒤로(ADR-33 D19 선례 — 운영자 파일명이 UNI 실패를 지어내 감사에 남았다). **검토 지적 전건 반영 완료**: F6 API e2e 18건 신설(그것을 쓰다가 **거부 감사가 롤백과 함께 사라지는** 결함을 또 잡았다 — US-SIT-009 E-01의 UPLOAD_REJECTED가 남지 않았다), KNOW 계약 게이트 13건 신설(어휘 네 개의 삼중 대조 + 오류코드 양방향 대조 + 202/닫힌 스키마), 증거문서, 사실과 다르던 마이그레이션 주석 세 곳 정정. | ADR-36; docs/evidence/CC-220-knowledge-upload-and-uni-adapter.md; contracts/openapi/uni-knowledge-api-change-request-v1.yaml; docs/handoff/UNI_KNOWLEDGE_API_REQUEST.md | PR 생성 후 머지; then CC-230 (EvidenceSet) |
| CC-230 | Implement evidence search and immutable EvidenceSet | DONE | feature/CC-230 | UNE-KNOW-004~007(검색·조회·동결·원문위치). **화면 흐름 상태를 저장하지 않는다** — US-SIT-011은 아홉 개를 말하지만 저장은 DRAFT/FROZEN 둘이다. `SEARCHING`은 요청이 진행 중이라는 뜻이라 요청이 끝난 뒤 그 행이 무엇을 뜻하는지 말할 수 없고 `NO_RESULTS`는 항목 0건인 DRAFT다(ADR-36 D1과 같은 판단). **검색은 동기다** — 설계 10 §7.25 7단계의 "Worker가 호출한다"는 모든 화면에 붙는 공통 문구이고, US-SIT-011은 대화형이며 설계 08 §1.14가 30초·1회다. CC-220 업로드(60초+비동기)와 갈리고 그래서 워커 권한이 필요 없다. **authorization filter는 응답 쪽에 있다** — 요청 범위만 걸고 응답을 믿으면 저쪽 필터가 틀렸을 때 알 수 없다. 버린 수를 응답에 싣는다. **마이그레이션 0031**: evidence_set/evidence_item에 어휘·상관식·FK 신설(0004에 제약이 하나도 없었다)이고 **RLS 정책이 0004부터 한 번도 없었다** — 0011의 일괄 GRANT 때문에 정책 없는 테이블은 전 테넌트 공개였고 CC-230이 첫 쓰기 경로를 연다(0023이 상황 계열에서 발견한 것과 같은 자리). frozen_at/frozen_by 신설(만든 사람과 동결한 사람을 구분할 수 없었다), is_selected 신설(제외하려면 지우는 수밖에 없어 "왜 뺐는가"가 사라졌다). **이중 검토 BLOCKER 2건, 둘 다 실측 재현 후 수정**: ① `...filters`가 전개 마지막이라 사용자가 `{"doc_ids":[]}`로 **UNI 요청의 범위 제한을 지울 수 있었다** — 응답 청크는 걸러지지만 원문은 걸러지지 않아 남의 기관 문서 본문이 요청자 테넌트의 provider_result에 적재되고 minimizePii도 우회됐다. CR-UNI-008에 filters가 정의돼 있지 않으므로 아예 보내지 않는다 ② **동결된 집합을 지울 수 있었다** — 집합 트리거가 UPDATE 전용이라 부모를 지우면 cascade가 근거를 데려가고 자식 가드는 부모가 사라져 NULL을 읽고 통과했다(0011이 "evidence_set 불변은 CC-230까지 애플리케이션 계층"이라 넘긴 숙제를 절반만 닫은 상태였다). DELETE 가드 + fail-closed + REVOKE DELETE. 그 밖에 반영: `score numeric(8,6)` 오버플로가 22003으로 **원문까지 롤백**시켰고(ADR 수용한계 7이 "DB가 검사하지 않는다"고 적은 것이 거짓이었다 — 정밀도를 풀었다), Snapshot 기준선에 **30초 TOCTOU 창**(읽기 트랜잭션에서 확인하고 커밋한 뒤 UNI 호출 중 확정이 들어오면 낡은 판 위 EvidenceSet이 동결된다 → 쓰기 트랜잭션에서 FOR UPDATE 후 재확인), 종료된 상황 가드 누락, 412 계약 미선언(재발 방지 게이트 추가 — 선언한 오류코드의 HTTP 상태가 responses에 있는지 검사), searchEvidence capability 미등록, UNI 실패 경로 테스트 0건(mock 시나리오 추가). 게이트: build·typecheck·lint·format PASS, contracts·intake·handoff PASS, 데이터사전 63/620. 테스트 skip 7(MinIO 미기동, 기존) — domain 194 · db-integration 169 · provider-adapters 205 · hwpx-engine 426 · api 416 · worker 64 · contract 248 · e2e 13 · web 28. 마이그레이션 30→31, 테이블 63 유지. | ADR-37; docs/evidence/CC-230-evidence-search-and-frozen-set.md | PR 생성 후 머지; then CC-240 (UNI SOP 생성) |
| CC-240 | Implement UNI SOP generation and versioned UniSopMapper | DONE | feature/CC-240 | UNE-SOP-001/002(생성 접수·SSE) + UniSopMapper + SopGraph 검증 + UNI SOP 포트·mock·실 HTTP 어댑터. **매퍼에 버전이 있다**(`uni-sop-1` → `sop_version.schema_version`) — 설계가 "UNI compns가 UNE 표준과 일치한다는 보장이 없다"고 적었으므로, 어느 규칙으로 옮겼는지가 남지 않으면 나중에 그래프가 이상할 때 UNI가 바꾼 것인지 우리가 잘못 옮긴 것인지 알 수 없다. 계약의 `schemaVersion:'1.0'`은 **다른 값**이며(클라이언트가 원하는 그래프 모양) 경계에서 `graphSchemaVersion`으로 이름을 바꿔 받는다. **매핑 실패는 경고이지 거부가 아니다**(설계 08 §1.11) — 스트리밍이라 `__compn__`이 하나씩 Canvas에 쌓이므로 필드 하나 때문에 응답 전체를 버리면 이미 그려진 노드까지 사라진다. 거부는 노드 단위 둘뿐(키 없음·모르는 유형). **검증 위반도 실패가 아니다** — COMPLETED + DRAFT 저장 + `graph_violations`. 저장하지 않으면 CC-250이 고칠 대상이 없다. **실측으로 찾은 결함 넷**: ① `sop_version`·`sop_node`·`sop_edge`에 **RLS 정책이 한 번도 없었다**(0008은 `sop`에만) — 0011의 일괄 GRANT 때문에 전 테넌트 공개였고 **이것이 세 번째다**(0023 상황 여섯, 0031 근거 둘, 0032 SOP 셋). ② **워커에 상황 계열 읽기 권한이 없었다** — 러너를 돌리자 `permission denied for table situation`으로 잡이 RUNNING에 멈췄다. 0032가 출력만 열고 입력을 잊었다. 0033이 SELECT만 열고 쓰기는 `situation.status` **한 열** + RESTRICTIVE 정책 (`CONTEXT_CONFIRMED→SOP_READY` 한 전이)으로 제한한다 — 테이블 UPDATE를 주면 워커가 `current_snapshot_id`까지 바꿀 수 있고 그것은 감사 없이 확정 사실을 갈아치우는 것이다. ③ `SELECT ... FOR UPDATE`가 **테이블 단위** UPDATE를 요구해 열 단위 GRANT로는 안 된다 — 잠금을 버리고 API 활성 잡 가드에 맡겼다(최악은 SOP 둘이지 데이터 손상이 아니다). ④ **`EDGE_FROM_END`** — mock 스트림이 찾아냈다. END 뒤 노드 하나가 순차 연결을 END로 **통과시키는데** DAG는 성립하고 END도 있어 CYCLE·NO_END 어느 것도 잡지 못했다. **노드 키 정규화**: 그래프 교환 스키마가 키를 `^[A-Za-z][A-Za-z0-9_-]{1,79}$`로 못박고 있는데 매퍼는 아무 문자열이나 받았다 — UNI가 `"3"`이나 한글 키를 주면 저장은 되고 CC-250 내보내기가 깨진다(그때는 쌓인 버전을 다 손봐야 한다). 정규화 + `providerNodeKey` 보존 + 경고, 해시에는 원본을 넣지 않는다. **SSE 프레이밍은 UNE 가정**(OB-04)이라 `.assumed` 표기이고 mock이 **가짜 SSE 본문을 실제 파서에 통과**시켜 그 가정을 실행한다. **공개 SSE 어휘는 UNE의 것**(`sop.node`/`sop.sources`) — provider 어휘를 흘리면 provider가 바뀔 때 클라이언트 계약이 깨진다. **계약 게이트가 자기 전제의 오류를 잡았다**: `UNI-503-003`이 "선언만 되고 아무도 던지지 않는 코드"로 보였으나 사실이 아니었다 — 비동기라서 `job.failed` payload 코드로 도착한다(SSE는 이미 200이다). 게이트가 워커 러너까지 읽도록 넓혔다. **이중 검토 BLOCKER 2건, 둘 다 실측 재현 후 수정**: ① **QUEUED SOP 잡을 취소할 수 없었다** — `/plan-jobs/{id}/cancel`이 SOP 잡의 aggregate_id(situationId)를 계획서로 찾다 PLAN-4003을 던지고 **그 404가 같은 트랜잭션의 CANCELLED 기록까지 되돌렸다**(측정: 404 + 잡은 QUEUED 그대로). SOP-409-001이 "취소하십시오"라고 안내하는데 그 취소가 막다른 길이었다 — PLAN 잡일 때만 상태를 되돌리고 SOP 전용 취소 **UNE-SOP-017**을 신설했다(ADR-38 D19) ② **잡 유형을 검사하지 않아 도메인 권한이 서로 통과했다** — SOP_READ만 가진 사용자가 계획서 본문 이벤트를 읽고 PLAN_GENERATE로 남의 SOP 잡을 끌 수 있었다. 조회·스트림·취소·재시도 네 곳에 허용 유형을 명시하고 404로 존재를 은닉한다(D20). 그 밖에 반영: 같은 키로 접히는 노드가 23505로 **트랜잭션 전체를 되돌려** 엉뚱한 MAX_ATTEMPTS_EXCEEDED로 끝나던 것, provider 제목 길이 미검증(22001 같은 경로), 0032가 워커에 **쓰지 않는 sop_version UPDATE**를 준 것(0034가 회수 — 기존 버전의 graph_hash·출처를 감사 없이 갈아치울 수 있었다), UNI DTO·매퍼가 도메인에 있어 **의존 방향이 뒤집힌 것**(어댑터 패키지로 이동, D18), 근거 범위 이탈을 검출할 데이터를 쥐고도 검출하지 않던 것(SOURCE_OUT_OF_SCOPE 신설 — ADR의 "사후에 잡지 못한다"가 회피 가능한 한계였다), provider 문서 id가 공개 계약·저장소로 그대로 나가던 것(UNE id로 투영), **설계 08 §1.14의 첫 이벤트 30초가 집행되지 않던 것**(검사가 read() 뒤에 있어 침묵 스트림에서 실상한이 5분이었다), 개행 없이 끝난 스트림이 정상 종료를 UNI_SOP_UNTERMINATED로 뒤집던 것, provider __error__ 원문이 공개 SSE로 나가던 것, 원문이 200KB를 넘으면 **전량 소실**되던 것, 계약이 선언한 SOP-404-002를 아무도 던지지 않던 것(게이트가 **정의가 아니라 호출**을 보도록 강화). 게이트: build·typecheck·lint·format PASS, contracts PASS, 데이터사전 63/627. 테스트: 도메인 26 · provider-adapters(uni/sop) 54 · 워커 e2e 14 · 통합 193(SOP RLS·워커 최소권한 18 포함) · 계약 265+(SOP 게이트 19) · API 슬라이스 e2e 17. 마이그레이션 31→34, 테이블 63 유지. | ADR-38; docs/evidence/CC-240-uni-sop-generation.md | PR 생성 후 머지; then CC-250 (SOP 캔버스·검증·승인) |
| CC-250 | Implement SOP canvas model, validation, review, and approval | DONE | feature/CC-250 | UNE-SOP-003~009(정의 생성·목록·그래프 조회·Draft 저장·검증·검토 요청·승인). CC-240이 만든 DRAFT를 **사람이** 고치고 확정한다. **`review_request`/`approval`을 도메인 전용 테이블로 만들었다**(ADR-39 D1, fable과 상의) — 설계 10은 세 도메인이 이름을 공유하지만 컬럼 수준 물리 설계가 없고, 공용으로 만들면 `generation_job`이 CC-240에서 낸 사고(타입 미검사로 권한 경계 관통)를 구조적으로 다시 부른다. 전용은 FK를 걸 수 있고 RLS가 부모 조인 하나다. 되돌리기 비용도 비대칭이다(전용→공용은 UNION 뷰, 반대는 append-only 이력 분할). **승인 시점 그래프 해시를 감사 행에 동결**하고(소급 보강 불가), 그것이 성립하려면 그래프도 불변이어야 하므로 LOCKED 트리거를 `sop_version`·`sop_node`·`sop_edge` 셋에 건다 — 버전만 막으면 내용은 바뀌는데 해시는 그대로여서 **승인한 것과 저장된 것이 달라진다.** 편집은 DRAFT에서만(검토 중 수정은 검토자가 본 것을 바꾼다), 저장은 덮어쓰지 않고 새 버전이며 `baseVersionId`로 동시성을 본다(409, 자동 덮어쓰기 없음). **검증 오류/경고를 "실행할 수 있는가"로 갈랐다** — 구조 위반은 전부 오류(고립 노드 포함), 매핑 경고는 승인을 막지 않되 `MISSING_TASK`만 예외. 400으로 막는 것은 **DB가 물리적으로 저장할 수 없는 것**뿐이다(중복 키 23505·없는 노드 간선·자기 간선·컬럼 폭 초과 22001). **RLS 누락을 절차로 막았다**: `sop_validation`이 네 번째였고, 전수 조사로 정책 없는 테이블 18개를 확인해 목록을 테스트에 고정했다 — 새 테이블이 정책 없이 들어오면 `rls-coverage.test.ts`가 먼저 빨개지고 목록은 줄어들기만 한다. 일곱 오퍼레이션의 응답이 전부 `SopRun` 자리표시자였던 것(실행 리소스가 캔버스·승인 응답으로)도 실체화했다. 게이트: build·typecheck·lint·format PASS, contracts PASS, 데이터사전 65/644. 테스트: 도메인 44 · API 단위 17 · 계약 게이트 16 · API 슬라이스 e2e 9 · 통합 197. 마이그레이션 34→35, 테이블 63→65. | ADR-39; docs/evidence/CC-250-sop-canvas-review-approval.md | PR 생성 후 머지; then CC-260 (SopRun·Task 상태기계) |
| CC-260 | Implement SopRun, Task, and explicit state machine | DONE | feature/CC-260 | UNE-SOP-010~016(모의·실행 시작, 상세, SSE, 일시중지·재개·강제종료). 승인된 버전을 실행해 임무를 만들고 활성화한다. **도달 가능한 상태만**(ADR-40 D1): 실행 넷(READY/RUNNING/PAUSED/TERMINATED)·임무 둘(CREATED/CANCELLED) — COMPLETED·FAILED와 SENT~COMPLETED는 그 값을 만드는 코드(CC-270/280)와 함께 온다. **DRY_RUN은 상황 상태를 건드리지 않고 살아 있는 실행으로 세지도 않는다** — 모의 때문에 대시보드·일지가 "대응 중"으로 보이면 그 화면을 믿은 사람이 잘못 판단한다. 모의는 READY에 머문다("시작했다"가 아니라 "준비됐다"). **"만들어진 임무"와 "지금 할 임무"를 갈랐다** — 상태로 구분하지 않고(설계 어휘에 "활성"이 없고 전파 상태와 섞으면 "보냈다"와 "해야 한다"가 한 값에 눌린다) `activated_at` 시각으로 남기며, 프런티어는 **저장하지 않고 계산한다**(커서를 두면 그래프·임무 상태와 어긋나고 어느 쪽이 참인지 말할 수 없다). 강제종료는 실행 id 앞 8자를 확인코드로 요구하고(오조작 방지 — 사용자가 화면에서 읽어 옮겨야 하므로 확인 자체가 대상 확인이 된다) 살아 있는 임무를 접는다. **실측으로 찾은 것**: `UPDATE execution_event`가 그대로 통과했다 — 0011이 권한만 회수했고 그것이 유일한 방어였다. CC-260이 사실원장의 **첫 기록자**이므로 append-only 트리거를 붙였다(task_event·sop_approval과 같은 두 번째 방어선). 종료된 실행의 임무도 DB가 막는다. RLS 커버리지 목록에서 `sop_run`·`task`·`task_event` 셋을 닫았다(남은 12개). 게이트: build·typecheck·lint·format PASS, contracts PASS, 데이터사전 65/645. 테스트: 도메인 62 · 계약 게이트 15 · API 슬라이스 e2e 8 · 전체 스위트 초록. 마이그레이션 35→36, 테이블 65 유지. | ADR-40; docs/evidence/CC-260-sop-run-and-task.md | PR 생성 후 머지; then CC-270 (Outbox·전파) |
| CC-270 | Implement Transactional Outbox and simulation channels | DONE | feature/CC-270 | UNE-TASK-003/013/014(전파 접수·상태 조회·재전파) + Outbox 릴레이 + 채널 포트·시뮬레이션 어댑터. **접수와 발송을 나눴다**(ADR-41 D1) — "상태변경·Execution Event·Outbox insert는 하나의 트랜잭션"이라는 비협상 규칙이 그 트랜잭션 안에서 채널을 부르는 것을 금지한다. API는 "보내기로 했다"를 커밋하고 워커가 큐를 읽어 보낸다. 계약 게이트가 그것을 강제한다(트랜잭션 본문에 `.send(`가 없어야 통과). **SYSTEM만 진짜이고 셋은 시뮬레이션이며 그 사실이 네 곳에 실린다**(어댑터·시도 기록·응답·기동 로그) — 시뮬레이션 성공을 "전파됐다"로 읽으면 아무도 받지 않은 지시를 받았다고 여긴다. **`DELIVERED`를 넣지 않았다**(수신영수증을 주는 채널이 없다 — 넣으면 영원히 도달하지 않는 값이 화면에 남는다). **부분 실패를 FAILED로 뭉개지 않는다**(절반이 받았는데 실패로 보이면 운영자가 전부 다시 보내고 받은 사람은 같은 지시를 두 번 받는다), **아직 진행 중이면 결론을 내지 않는다**(지금 PARTIAL이라 말하면 남은 것이 성공했을 때 그 말이 틀린 것이 된다). 재시도는 지수 백오프+상한(5분)+**결정적 지터**(몰림이 채널을 두 번째로 무너뜨린다)이고 재시도해도 같은 실패는 **바로** dead letter다. **`channel_delivery`를 만들지 않았다** — 설계가 이름을 쓰지만 그 정보는 `outbox_attempt`와 `dispatch_recipient`에 이미 있다(ADR-33 D4가 `malware_scan`에 내린 결론과 같은 형태). **중복 억제 키에 테넌트를 넣었다** — 0007의 인덱스에는 없어 두 기관이 같은 키를 쓰면 한쪽 전파가 조용히 사라졌다. **실측으로 찾은 것 둘**: ① RESTRICTIVE 정책에 `USING`만 써서 **릴레이가 줄을 종결하는 것 자체가 막혔다**(메시지가 SENDING에 머물렀다) — USING이 새 행에도 적용된다. `WITH CHECK (true)`로 닫았다 ② `task` 정책이 `sop_run`을 조인하는데 워커에 그 SELECT가 없어 42501 — 정책식은 질의하는 롤의 권한으로 돈다. RLS 커버리지 목록에서 `dispatch`·`dispatch_recipient`·`outbox_attempt` 셋을 닫았다(남은 9개). 게이트: build·typecheck·lint·format PASS, contracts PASS, 데이터사전 65/646. 테스트: 도메인 23 · 계약 318(전파 게이트 18) · API 슬라이스 e2e 9 · 통합 197 · 전체 스위트 초록. 마이그레이션 36→37, 테이블 65 유지. | ADR-41; docs/evidence/CC-270-outbox-and-dispatch.md | PR 생성 후 머지; then CC-280 (현장 임무 수행) |
| CC-280 | Implement field task acknowledge/start/progress/complete/reject/reassign | DONE | feature/CC-280 | UNE-TASK-001/002/004~012 + `apps/field-web` 현장 화면(모바일 우선·오프라인 대기열). **서명링크 인증을 만들지 않았다**(ADR-42 D1) — 설계 09가 `/task/:signedToken`을 적지만 지금 그 링크를 배달할 수단이 없다(채널이 전부 시뮬레이션이고 주소도 저장하지 않는다). 사용자가 0명인 bearer 경로는 공격면만 있고, 설계 10(우선순위 3)이 이 API 권한을 `TASK_ASSIGNEE`/`TASK_SUPERVISE`로 적어 문서 우선순위로도 이쪽이 이긴다. 되돌리는 비용도 비대칭이다(나중은 추가, 지금은 철거). **담당자 확인이 두 계층이다**(D2) — `TASK_ASSIGNEE`는 여러 현장요원이 함께 갖는 역할이라 권한만으로는 부족하고, 모든 전이가 `WHERE status AND assignee_user_id` 조건부 UPDATE다. 가드 통과 뒤 재배정되는 시간차 경합을 DB가 막는다(e2e가 그 창을 직접 만든다). **관측되지 않는 상태 셋을 넣지 않았다**(D4): `DELIVERED`(수신영수증 채널 없음, OB-06), `REJECTED`(반려하는 **순간** IN_PROGRESS가 된다), `REASSIGNED`(재배정하면 새 담당자의 SENT가 된다 — "기존담당 읽기전용"은 보는 사람 기준의 화면 표시다). 뒤의 둘은 `task_event`로 남고 화면이 마지막 이벤트에서 배지를 그린다. **수행 시각 컬럼을 두지 않았다**(D6) — `task_event`가 정본이고 둘을 두면 "언제 착수했는가"의 답이 둘이 된다. **완료조건은 조건을 적지 않은 SOP에도 결과 서술을 요구한다**(D7) — 빈 완료보고는 상황일지에서 빈 칸이 된다. 반면 첨부는 기본으로 요구하지 않는다(사진을 찍을 수 없는 임무가 실제로 있다). **마지막 임무가 승인되면 실행이 스스로 끝난다** — CC-260이 예고한 `sop_run.COMPLETED`. 다만 **수행불가로 남은 임무가 있으면 끝내지 않는다**(D9): 그것을 끝났다고 세면 아무도 하지 않은 절차 단계가 완료된 실행 안에 조용히 남는다. 알림(수행불가·반려·재배정·Escalation)은 CC-270 Outbox를 그대로 쓰고 **수행불가는 지휘자에게** 간다 — 담당자에게 보내면 자기가 낸 보고를 자기가 받는다(D10). 현장 앱의 오프라인 보장은 한 줄이다: **멱등 키를 넣을 때 한 번 만들고 재시도에서 그대로 쓴다**(D11). 그리고 화면은 "대기 중"이라고 적는다 — 보냈다고 표시하면 사람이 지휘소가 알고 있다고 믿는다. `task_assignment`(append-only, `released_at` 없음)를 더해 RLS 커버리지 목록에서 `task_attachment`를 닫았다(남은 8개, 실행·전파 계열은 전부 끝). 게이트: build·typecheck·lint·format PASS, contracts PASS, 데이터사전 66/654. **이중검토가 찾은 결함 열셋을 고쳤다**(ADR-42 D13~D15, 마이그레이션 0039): ① **알림 전파가 임무를 몰래 `SENT`로 만들었다** — 릴레이가 `dispatch.task_id`만 보고 종류를 안 봐서, 지시가 한 번도 안 나간 임무를 Escalation하는 것만으로 "전파됨"이 됐고 그 전이는 상태기계도 사실원장도 거치지 않았다. `TASK_NOTICE`를 열어 갈랐다 ② `advanceRun`이 실행을 잠그지 않아 **동시 승인 시 실행이 `RUNNING`에 갇혔다** ③ 0036 종료 트리거가 `COMPLETED`를 몰랐다 ④ **계약의 `SopRun.status`에 `COMPLETED`가 없는데 서버가 그것을 냈다 — 그것을 잡던 단언을 내가 지웠다**(복원) ⑤ SSE 공개 어휘에 `RUN_COMPLETED` 누락 ⑥ 이벤트 이력이 전부 **현재** 상태를 달고 나왔다(기록 시점에 굳힘) ⑦ 화면이 `requiresEvidence`를 몰라 서버가 422를 냈다 ⑧ 오프라인 큐가 누를 때마다 새 멱등키를 만들어 재입력 중복을 못 막았다 ⑨ 401을 "거절"로 다뤄 세션 만료 시 밀린 보고가 버려졌다 ⑩ 저장소가 테넌트를 RLS에만 맡겼다(ADR-21 보상통제 복원) ⑪ 첨부 정책이 파일 테넌트를 안 봤다 ⑫ `geo`가 형태 없는 임의 JSON이었다 ⑬ ADR·증거가 e2e가 증명하지 않은 것을 적었다(문장을 사실로 낮추고 시험 추가). 테스트: 도메인 298 · 현장앱 19 · 계약 352(CC-280 게이트 33) · API 434 · 통합 197 · E2E 90(CC-280 34) · 전체 스위트 초록. 마이그레이션 37→39, 테이블 65→66. | ADR-42; docs/evidence/CC-280-field-task-execution.md | PR 생성 후 머지; then CC-290 (Execution Log·대시보드 투영) |
| CC-290 | Implement Execution Log and dashboard projections | DONE | feature/CC-290 | UNE-JNL-001~004 + `apps/web` 전자상황판(REG-01~05). **사실원장을 처음으로 읽는다** — 다섯 항목이 이벤트를 쌓는 동안 그것을 보는 경로가 하나도 없었다. **판은 임무 행이 아니라 이벤트에서 계산된다**(ADR-43 D1). CC-280이 임무에 수행 시각 컬럼을 두지 않은 것과 같은 결정이고, 대시보드만 다른 정본을 쓰면 그 결정을 배반한다. 되돌리는 비용도 비대칭이다 — 임무 행 집계를 택하면 이벤트 완전성 압력이 사라져 그 기간의 과거가 **영구히** 복원 불가가 된다(성능은 가역, 데이터 결손은 비가역). e2e가 임무 행을 직접 바꿔도 판이 흔들리지 않음을 확인한다. **착수 전 측정에서 이벤트 스트림이 KPI를 지탱하지 못했다**(D3): 임무별 생성·취소 이벤트가 없고(실행 단위 요약만), **릴레이가 임무를 `SENT`로 올리면서 사실원장에 아무것도 남기지 않았다** — CC-280 H1과 같은 계열이고 재생이 정본이 되는 순간 기능적 결함이 된다. 셋 다 메운 뒤에 만들었다. **정정은 star다**(D4) — `corrects_event_id`가 항상 원본을 가리키고 정정의 정정은 DB와 API가 막는다. 사슬이면 "지금 사실"에 재귀가 필요하고 한 줄이 빠지면 답이 없다. 유효본은 O(1)이고 두 번 틀려도 원본을 다시 정정하면 된다. **입력은 부분 패치, 저장은 전체**(D5). **사람이 보고한 사실만 정정할 수 있다**(D7) — 시스템 관측 이벤트는 그 자체로 참이고, 잘못이면 기록을 고치는 것이 아니라 새 조치로 사실을 하나 더 만든다. `status`는 어떤 정정으로도 못 바꾼다(CC-280이 고친 결함을 정정 경로로 다시 들여오지 않는다). **원본을 감추지 않는다**(D8) — 집계는 정정본, 타임라인은 원본+표시, 상세는 셋 다. 정정이 **원본 해시를 싣고 쓰기 전에 대조**한다(D9). **응답이 자기 근거를 밝힌다**(D10, `provenance`) — 기한만 이벤트가 아니라 임무 행에서 온다는 사실을 화면이 판 위에 적는다. **투영 테이블을 만들지 않았다**(D11) — `journal_projection_item`은 CC-300의 것이고, 구체화하면 원본과 어긋날 때 어느 쪽이 참인지 말할 수 없다. 게이트: build·typecheck·lint·format PASS, contracts PASS, 데이터사전 66/654. **이중검토가 찾은 결함 열여섯을 고쳤다**(ADR-43 D12~D16, 마이그레이션 0041): ① **정정이 fold에 새 관측으로 섞여 완료된 임무를 진행으로 되돌렸다** — D8을 적어 놓고 `applyCorrections` 결과를 KPI가 아니라 `recentEvents`에만 썼다. 이제 집계는 정정본으로 접고 표시는 원본을 남긴다 ② **계약 스키마가 어떤 응답으로도 만족될 수 없었다** — allOf + `additionalProperties:false`(같은 파일에 ADR-24 D4로 금지가 명문화돼 있는데 그것을 밟았다). 스키마를 평탄화하고, **문서 전체 walker**와 **실응답 Ajv 검증**을 더했다 — 후자가 없어 CC-280의 `SopRun.status`도 새어 나갔다 ③ `recentEvents`가 원본 payload를 감추고 원본 해시와 어긋나게 냈다 ④ `TASK_CANCELLED`에 `runId`가 없어 실행 스코프에서 탈락(접힌 임무가 영원히 직전 상태) → payload가 아니라 DB에서 유도 ⑤ 과대 본문이 500 → 전역 상한 + 413 ⑥ 동시 정정이 서로를 삼켰다 → **자문 잠금**(append-only라 `FOR UPDATE`를 쓸 수 없다 — 잠글 수 없는 것이 이 테이블의 보장이다) ⑦ 워커 INSERT가 테이블 전체라 `corrects_event_id`가 딸려 왔다 → 컬럼 단위 ⑧ 릴레이가 이벤트 없이 성공 반환 → 예외로 롤백 ⑨ 근거로 든 인덱스를 아무 질의도 쓰지 않았다 + 0007과 중복 FK·인덱스 → 0041에서 정리 ⑩ 정정 값에 길이·깊이 제한 없음(append-only라 나중에 마스킹도 못 한다) ⑪ 정정에 확인 단계 없음 ⑫ `progressPct`가 임무 행에서 샜다 → 재생으로 ⑬ KPI→이벤트 drill-down 부재 → `statusEventId` ⑭ **ADR이 없는 e2e를 있다고 적었다** — "재생과 임무 행을 대조한다"고 써 놓고 그 시험이 없었다. `provenance.divergences`로 **매 조회가 D1의 전제를 측정하게** 하고 e2e가 그것을 단언한다 ⑮ 배경 서술 2건이 사실과 다름(0007이 이미 인덱스·FK를 만들었다) ⑯ `total`이 뺄셈 잔여값이라 체계적 결손이 숫자로 숨었다. 테스트: 도메인 323 · 워크스페이스 40 · 계약 390(CC-290 게이트 38) · API 434 · 통합 197 · E2E 111(CC-290 21) · 전체 스위트 초록. 마이그레이션 39→41, **테이블 66 유지**(새 테이블 없음). | ADR-43; docs/evidence/CC-290-execution-log-and-dashboard.md | PR 생성 후 머지; then CC-300 (상황일지 Projection) |
| CC-300 | Implement Journal Projection, locked facts, editing, and export | NOT_STARTED | - | - | Confirm dependencies and plan |
| CC-310 | Implement exercise close, evaluation, and improvement feedback | NOT_STARTED | - | - | Confirm dependencies and plan |
| CC-320 | Complete Situation-SOP-Journal vertical slice E2E | NOT_STARTED | - | - | Confirm dependencies and plan |
| CC-400 | Bind actual T3Q contracts and run contract/E2E tests | NOT_STARTED | - | - | Confirm dependencies and plan |
| CC-410 | Bind actual UNI contracts and finalize mapping | NOT_STARTED | - | - | Confirm dependencies and plan |
| CC-420 | Run Hancom Track B round-trip gate | NOT_STARTED | - | - | Confirm dependencies and plan |
| CC-430 | Security, performance, observability, backup and recovery | NOT_STARTED | - | - | Confirm dependencies and plan |
| CC-440 | Release candidate, acceptance package, and deployment runbook | NOT_STARTED | - | - | Confirm dependencies and plan |
