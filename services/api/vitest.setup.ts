/**
 * 테스트 전역 기본값 (CC-160).
 *
 * 앱은 기동 시점에 오브젝트 저장소 설정을 요구한다(설정을 빠뜨린 배포가
 * 첫 Export 요청에서야 실패하는 것을 막기 위해서다 — storage-factory.ts).
 * 그 fail-fast는 운영에서 옳지만, 저장소를 쓰지 않는 e2e까지 MinIO 설정을
 * 요구하게 만든다.
 *
 * 그래서 테스트에서는 인메모리 드라이버를 **기본값으로만** 둔다. 이미
 * 설정된 값이 있으면 손대지 않으므로, 실제 저장소를 겨냥한 테스트는
 * 자기 환경을 그대로 쓴다.
 */
process.env.OBJECT_STORAGE_DRIVER ??= 'memory';
