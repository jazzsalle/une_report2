# OPEN External Bindings

These items must not be guessed by Claude Code.

| ID | Binding | Current fallback | Completion evidence |
|---|---|---|---|
| OB-01 | T3Q auth, base URL, TLS, timeout, rate limit, error schema, RPT-002 SSE framing (contract only fixes `x-sse-done: '[DONE]'`; frame structure/heartbeat/event names are UNE assumptions — see tests/contract fixtures `.assumed.`) | LegacyT3qPlanAdapter (mock + unverified HTTP; provider 미검증) | signed interface sheet + contract test |
| OB-02 | T3Q current disaster situation API | SituationProviderPort disabled adapter | approved API contract |
| OB-03 | T3Q RPT-003 use for journal | UNE JournalProjection is owner | ADR amendment if enabled |
| OB-04 | UNI `/chat/json` SOP 생성 — **CC-410(2026-08-14)에서 대부분 닫혔다.** 실 UNI 3표본 측정 결과: ① **SSE 프레이밍은 UNE 가정이 맞았다** (`data:` 한 줄에 JSON 객체 하나, 객체의 키가 이벤트 이름, `[DONE]` 리터럴, `event:` 필드 없음, `content-type: text/event-stream`). 가장 앞선 차단이었던 항목이 확인으로 닫혔고 파일의 `.assumed` 표식을 뗐다(`uni-sop-sse.measured.ts`) ② **요청 본문 필드명 확인**: `{query, model_key?, top_k}` ③ **`doc_ids`는 존재하지 않는다** — 검색·생성 어느 쪽에도 범위 지정 필드가 없다. 문서 범위를 좁힐 수 없고, 노드에 출처도 없어 범위 이탈을 **검출할 수도 없다** ④ **담당 자리는 있다** — `compnAttrbSaveParamsList[].receiveOrgnztSns`/`receiveUserSns`(3표본 모두 빈 배열). "담을 자리가 없다"고 적었던 것은 틀렸다. **남는 미지수**: (a) **유형 코드 표** — `104001`=START/`104003`=ACTION/`104005`=DECISION은 3표본의 위치·구조로 읽은 추론이고 UNI가 준 것이 아니다 (b) **SSE 재접속 수단 없음** — `id:`·`retry:`·하트비트가 실측 0줄이라 Last-Event-ID로 이어받을 수 없다 (c) **`__sources__`에 문서 식별자가 없다**(`{filename, score, text}`뿐) — 노드↔근거를 이을 방법이 없다 | `uni-sop-2` 매퍼가 실측 어휘로 옮긴다. 실 응답 원문 3벌이 단위시험 픽스처로 고정돼 있다(`packages/provider-adapters/src/uni/__fixtures__/`). capability `sopGeneration=PROVIDER_DEV_VERIFIED`, 증거 `docs/evidence/CC-410-uni-contract-binding.md` | 유형 코드 표 + SSE 재접속 수단 + `__sources__`의 doc_id. 정본 **ADR-50** |
| OB-05 | SafeKorea/Naver legal and operational approval | feature flag off | policy/legal approval |
| OB-06 | **인도 차단이 아니다 — 사용자 결정 2026-08-14(ADR-49 D2): 시뮬레이션 채널만으로 진행한다.** 실제 발송은 어차피 막히므로 계약을 기다리지 않는다. 전제는 ADR-41 D2가 이미 세워 두었다 — SYSTEM만 진짜이고 나머지 셋은 시뮬레이션이며 **그 사실이 전파 상태에 드러난다**(시뮬레이션 성공을 "전파됐다"로 읽으면 아무도 받지 않은 지시를 받았다고 여긴다). **남는 위험**: 훈련·데모의 전파는 아무에게도 배달되지 않고, 현장 담당자는 로그인 경로로만 임무를 받는다. 계약이 확정되면 어댑터 배선·수신자 주소 보관·서명링크 계층(ADR-42 D1)이 함께 열려야 하므로 항목은 열어 둔다. ─ real SMS/email/messenger/broadcast contracts. **CC-280 영향(2026-08-12)**: 설계 09 SCR-TASK-001의 서명링크 인증(`/task/:signedToken`, `TASK-5201` 토큰 만료·`TASK-5202` 수신자 불일치)을 **구현하지 않았다** — 지금 그 링크를 배달할 채널이 없고(전부 시뮬레이션) 수신자 주소도 저장하지 않는다. 토큰 수명·회전·폐기 정책은 배달 채널의 성질(링크 길이 제한, 푸시 자체 인증, 주소 보관 여부)에 종속되므로 채널 계약 없이 정하면 전부 추측이다. 대신 JWT + `TASK_ASSIGNEE`/`TASK_SUPERVISE` + **담당자 본인 확인 2계층**으로 열었다(ADR-42 D1) | simulation/system channels; 현장 임무는 로그인 경로로만 접근 (그리고 그 로그인은 아직 mock SSO뿐 — OB-01) | channel interface contract + 서명링크 계층 재검토(ADR-42 D1) |
| OB-07 | validation institutions and natural/social scenarios | reference scenario packs | institution binding record |
| OB-08 | Hancom version for Track B (Windows QA env fixed to Windows 11 at CC-000) | Track A only | Track B test report |
| OB-10 | T3Q plan API CR-T3Q-001/002/003/004/009 acceptance | legacy RPT-001/002 + target-v2 full mock (CC-135: toc/content/job status/SSE/cancel/partial retry/semantic edit/capabilities — SSE 프레이밍·PARTIAL 종결성·409 코드 체계는 UNE 가정, 갭 매트릭스 §3) | signed change response + dev contract tests |
| OB-11 | T3Q evidence search and semantic validation CR-T3Q-005/006 | target-v2 mock adapter (CC-135: searchEvidence provenance 충전 + validateContent 휴리스틱 — 판정은 어떤 UNE 경로도 차단하지 않음, ADR-28 D9) | accepted contract + representative payloads |
| OB-12 | ProcessGPT adoption and rhwp pinned source decisions | ProcessGPT evaluation-only; rhwp still NOT imported — the provenance gate now exists and is CI-enforced (CC-140/ADR-29 D1: `pnpm validate:intake` R1..R11, green in the pre-intake state). Remaining: tag selection + actual intake | ADR + license/SBOM/hash/POC report |
| OB-13 | UNI 연동 — **차단 두 개가 CC-410(2026-08-14)에서 닫혔다.** ① multipart 파일 필드명 = **`file`**(`uploader`·`force`는 쿼리 파라미터) ② `/auth/login` 응답 토큰 필드명 = **`token`**(+`user` 객체). 덤으로 **로그인 요청 필드명이 `username`이 아니라 `account`**임이 드러났다 — 어댑터에 하드코딩돼 있던 유일한 추측이었고 실서버는 422로 거절한다. ③ `/search/` 규격도 실측했다: 요청 `{query, top_k}`, 응답 `{results:[{filename, score, text, doc_id}]}`, score **0..1**. **chunk id 없음**, 설계 09 REG-02/03의 **page/section 없음**, **`doc_ids` 범위 지정 없음**. **남는 미지수·결함**: (a) **`GET /documents/{doc_id}`가 라이브에 없다** — 어댑터가 그 경로로 상태를 묻는데 라이브 스펙에는 DELETE만 있다. 상태는 목록 `GET /documents/`에서 오고 어휘도 다르다(설계의 `QUEUED/PARSING/…`가 아니라 `"참고자료 생성 중"`/`"완료"` + `progress` 0~100). **지금 실 UNI에 붙이면 상태 조회가 전부 실패한다** (b) **UNE가 올린 문서를 지울 수 없다** — `DELETE`는 본문에 `{account, password}` 재인증을 요구하는데 `/auth/login`이 200으로 통과시킨 **같은 자격증명을 401로 거부한다**(실측). 멱등키 부재와 겹치면 재시도가 지울 수 없는 중복을 2만671건 공용 색인에 쌓는다 (c) **422가 제출 본문을 그대로 에코한다 — 비밀번호를 평문으로** (d) TLS 없음(평문 http) (e) 중복 업로드 멱등키 부재(§6) — 다만 목록 항목에 `file_hash`가 있어 논의 자리는 있다 | 실 어댑터가 실측값으로 동작한다(업로드·검색 200 확인). capability `knowledgeUpload`·`knowledgeSearch`=**PROVIDER_DEV_VERIFIED**, `knowledgeStatus`·`knowledgeReference`는 **UNE_ADAPTER_READY 유지**(위 (a) 때문에 승격 불가). UNE는 로그인 실패 본문을 저장하지 않으며 회귀 시험으로 못박았다 | 위 (a)~(e). 요청 문서는 `contracts/openapi/uni-knowledge-api-change-request-v1.yaml`·`docs/handoff/UNI_KNOWLEDGE_API_REQUEST.md`에 이미 있다. 정본 **ADR-50**, 증거 `docs/evidence/CC-410-uni-contract-binding.md` |
| OB-14 | **판단 기준이 정해졌다 — 사용자 결정 2026-08-14(ADR-49 D3): 고정 IP를 확보한 뒤 결정한다.** 후보 비교나 비용 검토가 선행이 아니라 고정 IP 확보가 선행이다(CC-000이 "개발자 PC에 고정 IP가 없다"를 이유로 데모 경로를 가른 것과 같은 축). 이 결정에 딸린 것 둘: **트레이스 수집기**(OTLP 엔드포인트, ADR-48 수용 한계 1)와 **오브젝트 저장소 백업**(대상 버킷·자격증명, ADR-48 수용 한계 3). ─ final delivery environment (demo backend host closed: Railway, see below) | demo backend on Railway containers; local Docker Compose for dev; frontend demo on Vercel | delivery env binding record |


## Closed bindings

| ID | Binding | Closed by | Evidence |
|---|---|---|---|
| OB-15 | 악성코드 검사(AV) 엔진·정책. `file_object.scan_status`는 영구 PENDING이고 UNE-DOC-002는 업로드 검증(크기·SHA-256 재계산·내용 기반 판정)만 한다 — 검증 통과를 CLEAN으로 올리면 하지 않은 검사를 했다고 감사에 남으므로 축을 분리했다(ADR-32 D3) | **사용자 결정 2026-08-14 — AV 엔진을 두지 않는다. 검사 결과 없는 파일이 UNI로 올라가는 것을 수용한다** | `UNE_KNOWLEDGE_ALLOW_SCAN_PENDING=true`가 이 프로젝트의 **승인된 배포 설정**이 됐다(지금까지는 "AV가 생길 때까지의 완화"였다). **바뀌지 않은 것이 더 중요하다**: `scan_status`는 여전히 PENDING에 머물고 **CLEAN으로 올리지 않는다** — 수용한 것은 "검사하지 않은 파일을 받는다"이지 "검사했다고 적는다"가 아니다. `malware_scan` 테이블도 만들지 않는다(값을 만드는 코드가 없다). **코드 기본값은 false로 남긴다** — 켜져 있다는 사실 자체가 수용 기록이고, 기본값을 뒤집으면 그 기록이 사라진다. 배포마다 `.env` 한 줄을 명시해야 하며 빠뜨리면 지식문서 등록이 fail-closed로 막힌다. **남는 위험**: 지식문서 파일은 어떤 악성코드 검사도 거치지 않고 UNI에 색인된다 — UNE는 크기·SHA-256·선언 MIME만 본다. 결정 정본 **ADR-49 D1**; `services/api/.env.example` |
| OB-17 | **DB 롤 멤버십이 프로비저닝되지 않는다 — 배포 전 차단 항목.** `une_app`은 `une_worker`로도 `une_retention`으로도 `SET ROLE`할 수 없었다(42501, 실측). initdb는 `une_app`만 만들고 두 대상 롤은 마이그레이션이 NOLOGIN으로 만드는데 그 사이를 잇는 GRANT가 저장소 어디에도 없었다(0015부터의 선재 결함). `.env.example`대로 워커를 띄우면 첫 트랜잭션이 42501로 죽고 보존 스윕은 한 번도 돌지 않았다 | **CC-320 후속 2026-08-13 — 워커 전용 로그인 롤 `une_worker_app`** | 마이그레이션 `0050_worker_login_role_membership.sql`(`GRANT une_worker, une_retention TO une_worker_app WITH INHERIT FALSE, SET TRUE` + 적용 확인 `RAISE EXCEPTION`), initdb `infrastructure/initdb/02-worker-role.sh`(LOGIN·비밀번호만 — 대상 롤은 그 시점에 아직 없다), `infrastructure/docker-compose.yml`·`infrastructure/.env.example`·`services/worker/.env.example` 동시 변경, 테스트 `tests/integration/src/worker-login-role.test.ts`(4). **`une_app`에 멤버십을 주지 않았다** — 기본 INHERIT가 정책 대상 자격까지 물려줘 API 런타임이 전 테넌트 원문을 보게 된다(ADR-35 D2/D4). `INHERIT FALSE`라 `SET ROLE`을 빼먹은 경로는 조용히 통과하지 않고 권한 오류로 드러난다. 테스트는 `SET ROLE`을 시도해 보지 않고 `pg_has_role`로 카탈로그에 묻는다 — 시도해 보는 방식으로 처음 썼더니 superuser 세션이라 **두 단언이 공회전했다**. 결정 정본 **ADR-47 D3·D4** |
| OB-19 | **지식문서 용도 파일 업로드 경로가 없다 — 지식→근거→SOP 생성 구간이 API만으로는 도달 불가능하다**(CC-320 V-1 실측). UNE-KNOW-001은 `fileId`를 받는데 그 `fileId`를 만들 API가 없었다(`IMPLEMENTED_PURPOSES`가 `HWPX_IMPORT` 하나, MIME도 HWPX 둘만) | **CC-320 후속 2026-08-13 — 용도별 업로드 정책 + `file_object.purpose`** | 마이그레이션 `0049_file_object_purpose.sql`, `services/api/src/document/file.service.ts`(용도가 MIME·크기·내용검사를 고른다), 소비 측 대조 — HWPX 반입은 `purpose='HWPX_IMPORT'`를, 지식문서 등록은 `checkKnowledgeFile`의 `PURPOSE_MISMATCH`를 본다. **지식문서의 형식 판정은 UNE가 하지 않는다** — PDF 매직바이트를 흉내 내면 UNI가 실제로 읽을 수 있는지와 무관한 판정이 한 벌 더 생기고 두 벌은 갈라진다(최종 판정은 `uni_status`). `ATTACHMENT`는 **열지 않았다** — 개인정보 최소화·EXIF 제거 판단이 선행이다. CC-320 수직 슬라이스가 이제 이 구간을 제품 경로로 지난다. **⚠ 남은 차단은 OB-15**: AV가 없어 `scan_status`가 영구 PENDING이므로 지식문서 등록에는 아직 `UNE_KNOWLEDGE_ALLOW_SCAN_PENDING=true`가 필요하다 — OB-19는 두 차단 중 하나만 없앴다. 결정 정본 **ADR-47 D1·D2** |
| OB-16 | 상황 수집 원문·요청의 보존기간·TTL (`provider_result.raw_payload_json`, `provider_job.request_json`) | 사용자 결정 2026-08-09 — **1개월(30일) 뒤 페이로드만 비운다** | 마이그레이션 `0026_situation_payload_retention.sql`(전용 롤 `une_retention`, 컬럼 단위 UPDATE, `redacted_at`, 롤 대상 RLS 정책), 러너 `services/worker/src/retention/payload-retention.runner.ts`(설정 `UNE_PAYLOAD_RETENTION_DAYS` 기본 30), 증거 `docs/evidence/OB-16-payload-retention.md`, 테스트 `services/worker/src/retention/payload-retention.runner.e2e.test.ts`(7) + `tests/integration/src/payload-retention-grants.test.ts`(13). **행은 지우지 않는다** — `payload_sha256`·`item_count`·수신시각·상태는 남아 "무엇을 받았다고 주장하느냐"에 답한다. `file_object`(오브젝트 저장소 객체를 함께 지워야 함)는 이 결정의 대상이 아니며 별도 판단으로 남는다. 결정 정본은 **ADR-35**. 마이그레이션 `0027_payload_redaction_transition_guard.sql`이 허용 전이를 하나로 고정한다(0026의 CHECK는 한 방향만 막아 원문 위조와 표식 삭제가 통과했다 — 실측). ~~**⚠ 배포 전 차단 항목(OB-17)**: `une_app`은 `une_retention`으로 `SET ROLE`할 수 없다~~ → **OB-17이 2026-08-13에 닫혔다**(마이그레이션 0050, 워커 전용 로그인 롤 `une_worker_app`). 스윕이 돌 수 있는 상태가 됐다 |
| OB-09 | repository URL, backend profile, CI, deployment target | CC-000 approval 2026-07-30 | `work-items/00_DECISIONS_TO_CONFIRM.yaml` (APPROVED), ADR-19; residual deployment items moved to OB-14 |
| OB-14 (demo backend host) | demo backend host among Render/Railway/Fly candidates | user decision 2026-07-30 | Railway (existing paid account); backend api/worker as Railway containers, managed PostgreSQL candidate Railway Postgres; final delivery environment stays OPEN under OB-14 |
| OB-18 | 훈련 만족도·잠재가치 설문(수집 경로) | 미구현. 설계 §3.9의 API 넷 어디에도 설문 제출 연산이 없고 `survey_response`는 §6에 테이블 정의조차 없다. 응답을 만들 코드가 없는 테이블을 만드는 것은 거짓 약속이므로 만들지 않았고 JNL-015의 `x-db-tables`에서도 뺐다(ADR-45 D6). 평가보고서는 부재를 1급 값으로 적는다 — `satisfaction.status = NOT_COLLECTED` + 사유. 활용성·잠재가치 정성 의견은 평가 종합의견과 지표 의견에 적힌다 | 설문 배포·수집·응답자 관리·개인정보 최소화(US-SIT-036 3단계) 설계 확정 + `survey_response` 신설 + 보고서에 집계 반영. 결정 정본 ADR-45 수용 한계 1 |
