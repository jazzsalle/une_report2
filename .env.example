# UNE platform local development only. Never commit real secrets.

# UNI POC
UNI_API_BASE_URL=http://10.20.10.101:8088
UNI_API_BASE_PATH=
UNI_OPENAPI_URL=
UNI_USERNAME=
UNI_PASSWORD=
UNI_API_KEY=
UNI_VERIFY_TLS=false

# T3Q legacy and target-v2
# WARNING: this value is the TRANSCRIPT host, kept for provider-bindings
# documentation only. No UNE service reads it, and it is NOT an approved
# call target — the worker's UNE_T3Q_BASE_URL has no default on purpose
# (OB-01); never copy this value there without an agreed binding.
T3Q_API_BASE_URL=https://plf.mois-disaster.t3q.ai
T3Q_MODEL_ID=ae894
T3Q_AUTH_TYPE=
T3Q_API_KEY=
T3Q_CLIENT_ID=
T3Q_CLIENT_SECRET=
T3Q_VERIFY_TLS=true
T3Q_PLAN_ADAPTER_MODE=mock-target-v2

# Optional ProcessGPT evaluation only
PROCESS_GPT_ENABLED=false
PROCESS_GPT_BASE_URL=
PROCESS_GPT_MCP_COMMAND=

# rhwp source intake
RHWP_UPSTREAM_REPOSITORY=https://github.com/edwardkim/rhwp
RHWP_UPSTREAM_TAG=
RHWP_UPSTREAM_COMMIT=
RHWP_ARCHIVE_SHA256=
